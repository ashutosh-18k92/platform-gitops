apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: rock-service-virtualservice
  namespace: super-fortnight
spec:
  hosts:
  - "rock-service.api.local"
  gateways:
  - "super-fortnight/super-fortnight-gateway"
  http:
  - match:
    - uri:
        prefix: /api
    route:
    - destination:
        host: "rock-service.super-fortnight.svc.cluster.local"
        port:
          number: 3001
    retries: # worst-case timeout attempts x perTryTimeout
      attempts: 3
      perTryTimeout: 2s
      retryOn: 5xx,reset,connect-failure,refused-stream
  - match:
    - uri:
        prefix: /health
    route:
    - destination:
        host: "rock-service.super-fortnight.svc.cluster.local"
        port:
          number: 3001
    timeout: 2s
