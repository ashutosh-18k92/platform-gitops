apiVersion: apps/v1
kind: Deployment
metadata:
  name: rock-service
  namespace: super-fortnight
  labels:
    app: rock-service
    version: v1
    team: rock-service-team
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rock-service
      version: v1
  template:
    metadata:
      labels:
        app: rock-service
        version: v1
        team: rock-service-team
      annotations:
        sidecar.istio.io/proxyCPU: 100m
        sidecar.istio.io/proxyMemory: 128Mi
        sidecar.istio.io/rewriteAppHTTPProbers: "true"
    spec:
      containers:
        - name: rock-service
          image: rock-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3001
              name: http
              protocol: TCP
          env:
            - name: PORT
              value: "3001"
            - name: SERVICE_NAME
              value: "rock"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
