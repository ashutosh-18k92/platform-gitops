apiVersion: v1
kind: Service
metadata:
  name: rock-service
  namespace: super-fortnight
  labels:
    app: rock-service
spec:
  type: ClusterIP
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: rock-service
