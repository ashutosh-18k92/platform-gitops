apiVersion: v1
kind: Service
metadata:
  name: paper-service
  namespace: super-fortnight
  labels:
    app: paper-service
spec:
  type: ClusterIP
  ports:
  - port: 3002
    targetPort: 3002
    protocol: TCP
    name: http
  selector:
    app: paper-service
